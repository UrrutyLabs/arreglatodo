{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && cd apps/api && pnpm db:generate && pnpm build"
  },
  "deploy": {
    "startCommand": "cd apps/api && pnpm exec prisma migrate deploy --config=./prisma/config.ts && pnpm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
